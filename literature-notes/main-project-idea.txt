This project will explore the notion that declarative memory (in humans) can serve as a basis for aesthetic judgement. We invoke a theory of decision making called instance-based learning as a macro theory of how one uses past instances, encoded in memory, as generating key information for a future decision.  And, we invoke Anderson's theory of needs probability of memory which has the supposition that the accessability of a specific memory instance is directly related to prior systematic statistical patterns, typically temporal, in the environment. (Anderson may be assuming an ergodic environment).  And, we are wondering how the inverted-u theory of stimulus complexity, coming from the theory of experimental aesthetics, might serve us in thinking about how memory might serve as the basis of aesthetic preference.  Is there a way to integrate all of these ideas?  I wonder.  You will see the References listed below for all of these topics. 

This project is, fundamentally, attempting to build a computational model of aesthetic judgement.  The computational basis is pyactup (see the file ./pyactup-info.text for details), a model of declarative memory encoding and retrieval.  In our initial theoretical sketch, each encoding of an instance (think instance-based learning) is implemented in pyactup (think ACT-R declarative memory).  Further, we will invoke the notion of a chunk in memory as the representation for each instance.  A chunk is a set of slot-value pairings where a slot is a variable and the value is the value of that variable for a given chunk.  Each IBL instance is encoded as a chunk.  And, by Anderson's memory theory, when an agent/person is confronted with a piece of music or a song, the agent can compute the difference between its activation in memory and the chunk in memory with the highest current activation.  This difference is the kernel of aesthetic judgment/evaluation. 


Algorithm A (differences in activation of chunks):
1. retrieve for prediction of next song the declarative memory chunk with the highest probability which, effectively is its current activation in memory.
2. Compute memory activation of song in the environment (not via retrieval but via an activation process lookup in pyactup).
3. compute the absolute value of the difference between 1 and 2.
4. label the output of 3 as the "aesthetic basis"

Algorithm A2 (extension of Algorithm A):
1. Compute steps 1-3 from evaluation A.
2. assign the output of step 1 to be x.
3. compute 1/e^x to be f(x).
4. compute the difference between the last two memory activations of the song in the environment (without a learning call, just check last two activations).  This will likely involve time which pyactup may give you in some form.  I.e., this computes the last two activations of the same song.  Assign this difference value to be y.
4. compute the mirrored sigmoid of y to be g(y)
5. multiply g(y) * f(x) and call it "evaluation"
6. encode (learn) a new chunk for the song with the lable of the song as one slot and the evaluation as another. 

Algorighm A3 (extension of Algorithm A):
Preliminary requirements:
A. this algorithm keeps a running average of what we have been calling "aesthetic basis".  It will be used as a central piece of the algorithm.  At its core it computes a time-averaged value of "aesthetic basis" as a simulation is running.  Further we parameterize it by the width of the time window for which it is computed. The parameterization we will need to discuss; one requirement is that there will be a mode to have a cumulative version of this time-average and there will also be a moving window version of this time-average.   Lets call it time-average aesthetic basis or something with that semantic. 
Algorithm steps:
1. Compute steps 1-4 from evaluaton A.
2. Update the "time-averaged aesthetic basis" using the current "aesthetic basis" as a new input to its calculation.
3. Compute the "evaluation" as inverted_parabola_left_anchore(x, r), where x is the output of step 1 (i.e., x is the current"aesthetic basis") and r is the new "time-averaged aesthetic basis" 
4. encode (learn) a new chunk for the song with the lable of the song as one slot and the evaluation as another. 

Algorithm B (similarity comparison):
1. NOT COMPLETED YET, BUT LIKELY SIMILAR TO Algorithm A but with similarity as a basis in place of chunk activation.  


Series of Experiments:
Design 1:  
Let us consider that aesthetic_basis as we are now computing as something we want to measure over time (as a time-series).  We want to understand how the aesthetic_basis varies in an agent given its environment.  For example, given a random, ergodic environment over a long-time horizon (say 10000 song exposures over say 100 songs), what is the time-averaged (say a window of 20 exposures) aesthetic_basis?  

Design 2: This is the same as design 1 except we are going to provide a systematic cyclic environment based on a radial function.  Imagine something like this as the controller for the environment.  We have all 100 songs as a discrete ordered set (not sure how to order, but doesn't really matter).  Over this ordered set, there is a probabilyt distribution of the probability that a song will serve as the next stimulus in the environment.  The location of this probability distribution (e.g., the center of the distribution) will vary systematically over the ordered set by some cyclic radioal function that has all the songs equally spaced on a circle.  You know what I mean?  It would be useful to visulize dynamically as a jpeg this relation of the ordered set, the distribution center and the radial funciton).   Does this make sense?

Design 3: THis is an expansion of Design 2 with significant changes.  Change 1: each chunk will have two slots, the name or identifier of the song and the evaluation of the song.  Change 2: Algorithm A2 (extension of Algorithm A) is described above and will serve as the basis for simulation for Design 3. Otherwise, I think we keep other components and features the same as Design 2.  

A few notes:
--turn noise to zero when running on some runs.


Design 4: INCOMPLETE THOUGTH< DO NOT IMPLEMENT.  Evo Game Theory approach where multiple songs compete and the strategy rules reference schedules of play for a song or set of songs. 

Design X: INCOMPLETE FOR FUTURE, DO NOT IMPLEMENT, CONSIDER THESE AS COMPONENTS OF FUTURE DESIGNS.  For example, can we have an agent decide to not encode a song in the environment (to ignore it) if it is either too far from predicted activation or is too low on the delta function (to mean is too familiar, see 1/1+e^delta_of_activation).

References
For the Anderson reflections notion (declarative memory chunks `representing the statistics in the environment) see: ./Anderson and Schooler - 1991 - Reflections of the Environment in Memory.pdf

For instance based learning see: ./Gonzalez et al. - 2003 - Instance-based learning in dynamic decision making.pdf

For pyactup information see: ./pyactu-info.txt

For theory of experimental aesthetics, see:  
./Güçlütürk and van Lier - 2019 - Decomposing Complexity Preferences for Music.pdf
./Chmiel2017_BackToTheInvertedU.pdf
./Orr and Ohlsson - 2001 - The Relationship Between Musical Complexity and Liking in Jazz and Bluegrass.pdf
./Orr-RelationshipComplexityLiking-2005.pdf

EOF